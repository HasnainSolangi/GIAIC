
let myName      : string    =  "Hasnain Ahmed";
let myAge       : number    =   25;
let isStudent   : boolean   =   true;


console.log(myName, myAge, isStudent);
console.log(myName.toUpperCase()); // Hasnain Ahmed

/*
Result: 
    Hasnain Ahmed 25 true
    HASNAIN AHMED

// In my case, I use the ESNext target, which is why my TypeScript code is compiled into JavaScript using `let` variables 
   instead of `var`. However, depending on your configuration, the resulting JavaScript code may vary."
*/
